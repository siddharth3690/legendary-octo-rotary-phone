import colors from './colors';

const theme = {
  light: {
    background: colors.background,
    text: colors.text,
    card: colors.primary,
  },
  dark: {
    background: '#000',
    text: '#FFF',
    card: '#1A1A1A',
  },
};

export default theme;
